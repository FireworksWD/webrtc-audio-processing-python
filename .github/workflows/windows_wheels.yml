name: Build Windows Wheels

on: [push, pull_request]

jobs:
  build_windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Build Tools
        run: |
          python -m pip install --upgrade pip setuptools wheel build
          pip install meson ninja pybind11 pybind11-global

      - name: Build C++ Library (MSVC)
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
          set CC=cl
          set CXX=cl
          :: 强制使用静态库编译，减少 DLL 缺失和链接符号报错
          meson setup build-win . --prefix="%CD%\install" --buildtype=release -Ddefault_library=static -Dcpp_std=c++20 --backend=ninja
          meson compile -C build-win
          meson install -C build-win

      - name: Build Python Wheel
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvarsall.bat" x64
          
          echo Preparing libraries for Windows build...
          if not exist "python\include" mkdir "python\include"
          xcopy /S /I /Y "install\include\*" "python\include\"
          
          :: 关键：拷贝所有生成的 .lib 文件（包括子模块库）到 python 目录
          copy /Y "install\lib\*.lib" "python\"
          
          :: 设置编译器和链接器环境变量
          set "INSTALL_DIR=%CD%\install"
          set "INCLUDE=%INCLUDE%;%INSTALL_DIR%\include;%INSTALL_DIR%\include\webrtc-audio-processing-2;%CD%\python\include"
          set "LIB=%LIB%;%INSTALL_DIR%\lib;%CD%\python"

          cd python
          :: 使用 --no-isolation 确保能读取上面设置的 LIB 和 INCLUDE
          python -m build --wheel --no-isolation --outdir ../dist

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: webrtc-python-windows-wheel
          path: dist/*.whl